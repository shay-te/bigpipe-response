bigpipe_response:
  rendered_output_path: ${output_path:data_output}
  static_uri:
  is_production_mode: true

  processors:
    jsx:
      source_paths:
        - ${source_path:data}
    css:
      source_paths:
        - ${source_path:data}
